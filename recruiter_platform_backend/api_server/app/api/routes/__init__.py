"""FastAPI route modules registered from ``app.main`` (prefix ``/api/v1`` unless noted).

- ``jobs``: CRUD for jobs
- ``candidates``: list/detail/upload CV and CV raster preview under ``/jobs/{{id}}/candidates``
- ``screening``: screening runs
- ``ws``: WebSocket screening progress
"""
